plugins {
    id 'application'
}

application {
    mainClassName = 'kellinwood.keystore.Convert'
}

version = project.property('convert-keystore.version')

dependencies {
    implementation 'org.bouncycastle:bcprov-jdk17:1.46'
    testImplementation 'junit:junit:5.0'
}

